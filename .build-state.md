# Phase 9 Build State

last_completed: PHASE 9 SHIPPED — agency backlog app (apps/backlog, port 3012). Per-tenant tabs, ADMIN_PASSWORD-gated, Claude-powered Suggest endpoint that scans .build-state.md + README + git log. Replaces ClickUp for Naples Digital agency work.
next_step: Deploy backlog as Railway service. Live-test against real Instantly + AssemblyAI tenants. Add Buffer/Publer auto-publish, Stripe billing, scheduled-send dispatcher cron.
last_commit: 4d9a3be

## Phase 9 (2026-05-06) — agency backlog tracker

- 0006_backlog.sql: backlog_items table (tenant_id, title, description, status, priority, source, tags, due_at, completed_at, sort_order); RLS; updated_at trigger; populated tenants.brand.accent_color (#E8192C for 239live, #4F7DB8 for naplesdigital)
- @naples/db: BacklogItem type + listBacklogItems / createBacklogItem / updateBacklogItem / deleteBacklogItem / bulkCreateBacklogItems
- apps/backlog (NEW): Next.js 14 standalone, port 3012, ADMIN_PASSWORD-gated like admin-console. Tab per tenant with accent stripe; quick-add row; inline edit; status/priority dropdowns; tag chips; status counts; filters by status/priority/tag; done items grayed and sorted to bottom.
- /api/backlog/suggest: Reads .build-state.md, README, recent git log, git status, current backlog → asks Claude Sonnet 4.6 for 4-8 new concrete items not yet captured. Falls back to deterministic scan of next_step line if ANTHROPIC_API_KEY unset.
- Pre-seed: 16 items for 239live (config debt P0, half-built P1, not-started P2, defer P3) + 6 items for naplesdigital (agency-site content, outbound, intake template, Vitalryze prep, case study, onboarding wizard).
- README updated with new app row. .env.example adds NEXT_PUBLIC_BACKLOG_URL + ADMIN_PASSWORD. sync-env.sh detects backlog service and applies the AI key.
- Global rule update (~/.claude/rules/rad-energy.md): Task tracking row changed from "ClickUp" to "In-platform backlog app per project (no external SaaS)".



## Phase 8b live URLs (added 2026-05-02)

- admin-console:        https://admin-console-production-12e2.up.railway.app
- outreach-dispatcher:  https://outreach-dispatcher-production.up.railway.app
- render-worker:        worker-only (no public URL; visible in Railway dashboard)

ADMIN_PASSWORD generated and stored in .env.local for admin-console login.

## Phase 8 (2026-05-02) — three commits on top of Phase 7

**Track 0 — multi-tenant chassis (commit 475898d):**
- 0003_tenancy.sql: tenants + tenant_users + tenant_integrations + tenant_id on 16 existing domain tables + RLS
- packages/db/lib/tenant.ts (chokepoint), next-tenant.ts (Next.js helpers)
- All 9 existing apps refactored: every query takes tenantId first arg
- packages/ui TenantBrandProvider for white-label
- apps/admin-console (NEW): Naples Digital ops UI — tenant list, create-tenant form, per-tenant integration manager (Instantly, Smartlead, Apollo, Clay, AssemblyAI, Stripe). Gated by ADMIN_PASSWORD.

**Track A — cold email outreach (commit 753b796):**
- 0004_outreach_activation.sql: outreach_sequences (vendor state machine), email_sends (per-attempt rows), lead_emails, lead_enrichment + new lead columns
- packages/outreach (NEW): vendor-agnostic OutreachVendor interface + Instantly + Smartlead implementations
- packages/enrichment (NEW): Apollo + Clay implementations
- crm-pipeline: 7 new API routes (lead CRUD, CSV import, full lead view, enrich, sequences) + LeadDrawer component + AddLeadModal
- apps/outreach-dispatcher (NEW): Railway webhook receiver for Instantly/Smartlead events. URL pattern: /api/webhooks/instantly?tenant=<slug>

**Track B — content pipeline (commit 25ee4f3):**
- 0005_content_activation.sql: episodes get raw_video_url + transcript jsonb + processing_state; clips get start/end_seconds + video_url + word_timestamps; render_jobs table
- packages/storage (NEW): tenant-prefixed Supabase Storage wrapper
- packages/transcription (NEW): AssemblyAI client
- client-portal: real upload flow — signs URL, PUTs file directly to Supabase Storage with progress, no more fake s3:// strings
- content-pipeline: /api/episodes/[id]/transcribe (AssemblyAI), /api/episodes/[id]/clips REWRITTEN (uses real transcript), /api/clips/[id]/video (signed playback URL)
- apps/render-worker (NEW): Railway service running ffmpeg + node. Polls render_jobs, downloads source, ffmpeg crop 9:16 + drawtext caption burn from word_timestamps, uploads result. One worker, N tenants.

## Verification

- Migration applied to Supabase; existing rows backfilled to '239live' tenant
- All 10 prior apps + 3 new apps build clean
- Tenant isolation verified: SQL probe shows `select tenant_id, count(*) from leads` clean per-tenant
- Two tenants seeded: '239live' (primary, Kevin) + 'naplesdigital' (Naples Digital itself)

## ⚠️ Important — config still needed before live use

- ADMIN_PASSWORD env var on admin-console Railway service
- ASSEMBLYAI_API_KEY env var on render-worker + content-pipeline Railway services (or per-tenant via admin-console)
- INSTANTLY_API_KEY per tenant via admin-console → Integrations
- DNS for *.naplesdigital.app subdomain wildcard cert (optional — path-based /t/<slug>/ works zero-config)
- Deploy 3 new Railway services: admin-console (port 3010), outreach-dispatcher (port 3011), render-worker (no port, worker)

## Phase progress (Phase 8)

- [x] T0 — multi-tenant chassis
- [x] TA — outreach activation (Instantly/Smartlead wrap)
- [x] TB — content pipeline (Supabase Storage + AssemblyAI + ffmpeg/Remotion render worker)

---

# Phase 7 Build State (preserved)

last_completed: PHASE 7 SHIPPED — 10 services live, full redesign + client-portal + booking time picker + CRM rename
last_commit: 23a07b8

## Phase 7 (2026-05-01) added on top of Phase 6
- Full broadcast-red rebrand — Bebas Neue + Inter + #E8192C across all 10 apps (system-wide tokens.ts + tailwind-preset.ts swap, plus all chart hex values)
- Booking-portal step 2 now picks date AND time (8 AM – 9 PM hourly slots, mock-booked per-date); time persisted to bookings.time
- Display rename CRM → Lead Management (Nav, sidebar, page heads); folder still crm-pipeline
- New 10th app `client-portal` with DocuSign-style signing, Stripe-style billing, Opus-style content submissions
- Schema migration 0002: bookings.time + contracts + invoices + content_submissions tables
- 239live-site (public) redesign — lifted client-portal aesthetic onto all 3 routes (/ /studio /shows), new SiteNav with red "Book Now" pill
- Dockerfile fix: all 7 original Dockerfiles now COPY packages/db (was breaking rebuilds)

## Verification (2026-05-01)
- 10/10 Railway services return 200
- Supabase: 10 bookings, 14 leads, 7 episodes; 1 demo sponsor + 12 weeks of metrics; 2 contracts + 3 invoices + 3 submissions for Bonita Bay Group (contracts@bonitabay.com)
- Empty: clips, outreach_runs, sponsor_pitches — these populate as Kevin uses the AI features during the demo

## Demo flow for the pitch
1. Open dashboard → show real-data KPIs and charts
2. Open crm-pipeline → drag a card, refresh to prove persistence; click ✨ Angle on any card to show AI lead intelligence
3. Open content-pipeline → click 🎬 Clips on any episode to show AI clip generation; mark a clip posted to show the round-trip
4. Open outreach-demo → generate a sequence (still the strongest live AI moment)
5. Open sponsor-pitch → input a real sponsor candidate, generate a one-pager
6. Open sponsor-analytics → /s/b0a90296-f7b8-4579-a5e3-0dba99893afa shows the Naples Yacht Club demo portal
7. Back to dashboard /revenue to show the Sponsors section with the magic-link copy button

## ⚠️ Important
- ANTHROPIC_API_KEY is NOT yet set on any Railway service. All AI features will use mock fallbacks until Jake adds it. To set:
  `ANTHROPIC_API_KEY=sk-ant-... bash scripts/sync-env.sh`
- Service role + anon Supabase keys were pasted in chat history — rotate via Supabase dashboard → Settings → API after the demo

## Phase progress

- [x] A1 — env wiring (.env.local, .env.example, sync-env.sh)
- [x] A2 — @naples/db package (server.ts, queries.ts, types.ts)
- [x] A3 — schema migration (13 tables + RLS)
- [x] A4 — seed Supabase from MOCK_* (10 bookings, 14 leads, 7 episodes, 1 mrr, 12 social, 7 projections, 3 roadmap, 1 sponsor, 12 sponsor_metrics)
- [x] A5 — checkpoint + commit
- [x] B1 — booking-portal write path
- [x] B2 — crm-pipeline read+write
- [x] B3 — content-pipeline read+write
- [x] B4 — dashboard real reads
- [x] B5 — outreach-demo logging
- [x] B6 — checkpoint + commit + redeploy
- [x] C1 — CRM Lead Intelligence
- [x] C2 — Clip Generator
- [x] C3 — Sponsor Pitch Builder (new app)
- [x] C4 — Sponsor Analytics Portal (new app)

## Notes for resumer

- Supabase project ref: ylqoxefiwwimzxeuzfxy
- Demo sponsor magic_link_token (used in pitch demo for sponsor analytics portal):
  `b0a90296-f7b8-4579-a5e3-0dba99893afa` (sponsor: Naples Yacht Club)
- All env vars already on all 7 Railway services (verified via sync-env.sh)
- Anthropic key NOT yet set on Railway — outreach-demo + AI features will fall back to mock generators until Jake adds it. Adding ANTHROPIC_API_KEY to Railway is a single command and can happen any time without redeploy.
- @naples/db package exports: createServerClient, hasSupabase, listBookings, listLeads, listEpisodes, getMrr, getSocialGrowth, getProjections, getRoadmap, getOutreachStats, createBooking, updateLeadStage, cacheLeadAngle, getLeadById, createEpisode, getEpisodeById, logOutreachRun, listClipsForEpisode, createClips, markClipPosted, createSponsorPitch, listSponsorPitches, getSponsorPitch, getSponsorByToken, listSponsors
- All queries fall back to MOCK_* if hasSupabase() returns false — apps still render in dev without keys
- Plan file: /Users/jacobpaine/.claude/plans/so-we-are-almost-atomic-wolf.md

## How to resume

1. Read this file's `next_step` field
2. Read the plan file for the detailed step description
3. Continue from `next_step` through C4
4. Update this file after each step
5. Commit + push after each named step (B1–B5 individually, then B6 batch; C1–C4 individually)
